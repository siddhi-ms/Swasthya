# Backend (Node + Express)

Run these commands from the `backend` folder:

```bash
npm install
npm run dev
```

The backend listens on port 4000 by default and exposes a simple health endpoint at `/api/health`.
