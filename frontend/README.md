# Frontend (Vite + React)

Run these commands from the `frontend` folder:

```bash
npm install
npm run dev
```

The dev server will start on http://localhost:5173 by default. API requests to `/api/*` are proxied to http://localhost:4000 during development.
